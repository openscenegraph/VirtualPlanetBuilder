INCLUDE_DIRECTORIES(${GDAL_INCLUDE_DIR} ${OPENSCENEGRAPH_INCLUDE_DIRS} ${LIBSQUISH_INCLUDE})
SET(TARGET_LIBRARIES_VARS GDAL_LIBRARY OSG_LIBRARY OSGVIEWER_LIBRARY LIBSQUISH_LIBRARY)

SET(TARGET_SRC vpbtexturecompression.cpp )

SETUP_EXAMPLE(vpbtexturecompression)
